[project]
name = "download-data"
version = "0.1.0"
description = "Download MobileNetV2 model and generate test images for benchmarks"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
download-data = "download_data:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"