[project]
name = "download-test-assets"
version = "0.1.0"
description = "Download ONNX models and generate test images for testing"
requires-python = ">=3.10"
dependencies = []

[project.scripts]
download-test-assets = "download_test_assets:main"

[build-system]
requires = ["hatchling"]
build-backend = "hatchling.build"